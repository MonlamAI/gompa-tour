# Copy this file to `.env` at the project root before running the app.
# The `.env` file is gitignored and must not be committed.

BASE_URL=https://api.neykor.net
IMAGE_BASE_URL=https://s3.ap-south-1.amazonaws.com/gompa.tour
